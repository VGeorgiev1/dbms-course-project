doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
    title Sign Up Form with live validation
    link(rel='stylesheet', href='regstyle.css')
  body
    form(action='login', method='post')
      h2 Sign In
      p
        label.floatLabel(for='Username') Username
        input#username(name='username', type='text')
      p
        label.floatLabel(for='password') Password
        input#password(name='password', type='password')
        span Enter a password longer than 8 characters
      p
        input#submit(type='submit', value='Create My Account')
    script(src='http://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js')
